import {StyleSheet} from 'react-native';
import {fonts} from '../../../constants/fonts';

export default StyleSheet.create({
  mainWrapper: {
    margin: 20,
  },
  title: {
    fontFamily: fonts.MacPawFixelDisplayThin,
  },
});
